# Project Rules

- This project is a standalone browser-only Vite application.
- Keep GitHub Pages subpath deployment working by using the configured Vite `base`.
- Do not add server-side processing or external runtime assets unless explicitly needed.
- Use Playwright-driven browser checks for functional and visual QA before claiming verification.
